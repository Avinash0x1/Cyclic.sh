workflows:
  main:
    scripts:
      - name: Install dependencies
        script: npm install
      - name: Build the app
        script: npm run build
      - name: Run the entrypoint script
        script: sh entrypoint.sh
      - name: Start the app
        script: npm start
